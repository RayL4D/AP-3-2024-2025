import { createApp } from 'vue';

// Importez le composant principal (par exemple, App.vue)
import App from './components/HelloWorld.vue';

// Montez l'application sur un élément HTML (par exemple, un div avec l'id "app")
createApp(App).mount('#app');
